export type PersonaResumen = { _id: string; nombres: string; apellidoPaterno: string; apellidoMaterno?: string; ci: string; email?: string; telefono?: string; fotoPerfil?: string };
export type GestionResumen = { _id: string; nombre: string; anio?: number };
export type EstadoTerminosFraterno = "ACEPTADOS" | "PENDIENTE" | "NO_ACEPTADOS" | "REQUIERE_NUEVA_ACEPTACION";
export type Fraterno = { _id: string; usuarioCreador?: PersonaResumen | string | null; preregistroId: string | { _id: string; numeroPreRegistro: string }; usuarioId: string | PersonaResumen; gestionId: string | GestionResumen; numeroFraterno: string; fechaIngreso: string; estado: "ACTIVO" | "INACTIVO" | "RETIRADO"; situacion?: string; ocupaCupo?: boolean; estadoPago?: string; cuota?: { _id: string; montoTotal: number; montoPagado: number; saldo: number; numeroCuotasElegidas?: 1|2|3; fechaVencimiento?: string; fechaInicioPlazo?: string; fechaProrroga?: string; motivoProrroga?: string } | null; pagos?: Array<{ _id:string; numeroPago:number; monto:number; metodoPago:string; baucherImagen?:string; fechaPago:string; fechaCreado:string; estadoRevision:string; observacionRevision?:string; usuarioRevisor?:PersonaResumen }>; plazo?: { fechaInicio?:string; fechaVencimiento?:string; horasRestantes:number|null; prorrogaActiva:boolean }; terminos?: { estado:EstadoTerminosFraterno; versionVigente:number|null; versionAceptada:number|null; fechaAceptacion:string|null; contenidoAceptado:string|null; ip?:string; userAgent?:string; historial:Array<{version:number;fechaAceptacion:string}> } };
export type Traspaso = { _id: string; preregistroId: string | { _id: string; numeroPreRegistro: string }; usuarioOrigenId: string | PersonaResumen; usuarioDestinoId: string | PersonaResumen; montoTotalCuota: number; montoPagadoAlTraspaso: number; saldoAlTraspaso: number; motivo: string; observacion?: string; estado: string; fechaSolicitud: string };
export type Asistencia = { _id: string; fraternoId: string | { _id: string; numeroFraterno: string }; usuarioId: string | PersonaResumen; gestionId: string | GestionResumen; fechaClave: string; fecha: string; horaEntrada: string; horaSalida?: string; estado: "PRESENTE" | "INCOMPLETA" | "ANULADA" };
