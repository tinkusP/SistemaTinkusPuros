export type EstadoCuota = "PENDIENTE" | "PAGO_PARCIAL" | "PAGADA" | "VENCIDA" | "CANCELADA";
export type EstadoRevisionPago = "PENDIENTE" | "VERIFICADO" | "OBSERVADO" | "RECHAZADO";
export type Cuota = { _id: string; resumenPagos?: { cantidad: number; pendientes: number; conBaucher: number; ultimoEnvio?: string }; primeraCuotaMonto?: number; numeroCuotasElegidas?: 1 | 2 | 3; tipoOrigenTarifa?: "INTERNO" | "EXTERNO"; tarifaAplicada?: number; preregistroId: { _id: string; numeroPreRegistro: string; estado?: string; usuarioId?: { nombres: string; apellidoPaterno: string; apellidoMaterno?: string; ci: string; email: string; telefono?: string; fotoPerfil?: string; tipoOrigen?: string }; gestionId?: { nombre: string; anio: number } }; montoTotal: number; montoPagado: number; saldo: number; estado: EstadoCuota; fechaVencimiento?: string; fechaInicioPlazo?: string; fechaSolicitudProrroga?: string; cupoLiberado?: boolean; fechaLiberacionCupo?: string; fechaProrroga?: string; horasProrrogaAcumuladas?: number; motivoProrroga?: string; observacion?: string; fechaCreado: string };
export type MetodoPago = "EFECTIVO" | "QR" | "MIXTO";
export type DetalleCuota = { _id: string; cuotaId: string; numeroPago: number; monto: number; metodoPago: MetodoPago; montoEfectivo: number; montoQr: number; baucherImagen?: string; respaldoAdminImagen?: string; nombrePagador: string; fechaPago: string; estadoRevision: EstadoRevisionPago; observacionRevision?: string; fechaRevision?: string; usuarioRevisor?: { nombres: string; apellidoPaterno: string }; fechaCreado: string };
