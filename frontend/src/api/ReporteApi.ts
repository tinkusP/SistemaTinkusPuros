import api from"@/lib/axios";import{obtenerMensajeError}from"./apiError";export type PersonaReporte={usuarioId:string;nombre:string;ci:string;email:string;genero:string;facultad:string;carrera:string;origen:string;estadoUsuario:string;estadoPreregistro:string;cuota:{clasificacion:string;montoTotal:number;montoPagado:number;saldo:number;cantidadPagos:number}};export type Reporte={generadoEn:string;gestion:{_id:string;nombre:string;anio:number};resumen:Record<string,number>;finanzas:Record<string,number>;distribuciones:Record<string,{nombre:string;total:number}[]>;personas:PersonaReporte[]};export async function obtenerReporteEjecutivo(){try{return(await api.get<Reporte>("/reportes/ejecutivo")).data}catch(e){throw new Error(obtenerMensajeError(e,"No se pudo generar el reporte"))}}export async function obtenerAlmacenamiento(){try{return(await api.get("/reportes/almacenamiento")).data as any}catch(e){throw new Error(obtenerMensajeError(e,"No se pudo medir el almacenamiento"))}}
export async function obtenerReporteTallas(){try{return(await api.get("/reportes/tallas")).data as {registros:any[]}}catch(e){throw new Error(obtenerMensajeError(e,"No se pudo generar el reporte de tallas"))}}
export async function obtenerReporteFormacion(){try{return(await api.get("/reportes/formacion")).data as {guias:any[];postulantes:any[]}}catch(e){throw new Error(obtenerMensajeError(e,"No se pudo generar el reporte de guías"))}}

