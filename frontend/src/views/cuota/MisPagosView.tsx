import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { Link } from "react-router-dom";
import { toast } from "react-toastify";
import { obtenerMiCuota, registrarPago } from "@/api/CuotaApi";
import type { MetodoPago } from "@/types/CuotaType";
import { aceptarTerminosPago, miConfiguracionPago } from "@/api/ConfiguracionPagoApi";
const API = String(import.meta.env.VITE_API_URL || "").replace(/\/api\/?$/, "");

export default function MisPagosView() {
  const qc = useQueryClient(); const q = useQuery({ queryKey: ["mi-cuota"], queryFn: obtenerMiCuota, retry: false });
  const configQr = useQuery({ queryKey: ["mi-configuracion-pago"], queryFn: miConfiguracionPago, retry: false, enabled: Boolean(q.data) });
  const [modalTerminos,setModalTerminos]=useState(false); const[tipoQr,setTipoQr]=useState<"TOTAL"|"PRIMERA"|"SEGUNDA">("PRIMERA");
  const aceptar=useMutation({mutationFn:()=>{const g=configQr.data!.configuracion.gestionId;return aceptarTerminosPago(typeof g==="object"?g._id:String(g))},onSuccess:async(r:any)=>{toast.success(r.message);setModalTerminos(false);await qc.invalidateQueries({queryKey:["mi-configuracion-pago"]})},onError:(e:Error)=>toast.error(e.message)});
  const [form, setForm] = useState<{ monto: number; metodoPago: MetodoPago; montoEfectivo: number; montoQr: number; nombrePagador: string; fechaPago: string; baucher: File | null }>({ monto: 0, metodoPago: "QR", montoEfectivo: 0, montoQr: 0, nombrePagador: "", fechaPago: new Date().toISOString().slice(0, 10), baucher: null });
  const pagar = useMutation({ mutationFn: () => registrarPago(q.data!.cuota._id, form), onSuccess: async () => { toast.success("Pago enviado. La administración revisará la información."); setForm({ ...form, monto: 0, montoEfectivo: 0, montoQr: 0, baucher: null }); await qc.invalidateQueries({ queryKey: ["mi-cuota"] }); }, onError: (e) => toast.error(e instanceof Error ? e.message : "No se pudo registrar") });
  const actualizarMetodo = (metodoPago: MetodoPago) => setForm({ ...form, metodoPago, montoEfectivo: metodoPago === "EFECTIVO" ? form.monto : 0, montoQr: metodoPago === "QR" ? form.monto : 0, baucher: metodoPago === "EFECTIVO" ? null : form.baucher });
  if (q.isLoading) return <div className="min-h-screen bg-[#eee8dc] p-8 text-center">Cargando tu cuota...</div>;
  if (!q.data) return <div className="min-h-screen bg-[#eee8dc] p-5"><div className="mx-auto max-w-xl rounded-2xl bg-white p-8 text-center"><h1 className="text-2xl font-black text-[#74122A]">Todavía no tienes una cuota asignada</h1><p className="mt-2 text-sm text-slate-500">Cuando administración cree tu cuota aparecerá aquí.</p><Link to="/comunicados" className="mt-5 inline-block font-bold text-[#74122A]">← Volver</Link></div></div>;
  if (q.data.listaEspera) return <div className="grid min-h-screen place-items-center bg-[#eee8dc] p-5"><section className="max-w-xl rounded-3xl border border-amber-300 bg-white p-8 text-center shadow-xl"><div className="text-5xl">⏳</div><h1 className="mt-4 text-3xl font-black text-[#74122A]">Estás en lista de espera</h1><p className="mt-3 text-slate-600">El plazo para pagar tu primera cuota terminó sin que exista un pago verificado. Tu cupo reservado fue liberado.</p><p className="mt-3 rounded-xl bg-amber-50 p-4 text-sm font-bold text-amber-900">Comunícate con administración para consultar si puede habilitarse nuevamente un cupo y un nuevo plazo de pago.</p><Link to="/comunicados" className="mt-5 inline-block font-bold text-[#74122A]">← Volver a comunicados</Link></section></div>;
  const { cuota, pagos } = q.data;
  const limite = cuota.fechaVencimiento ? new Date(cuota.fechaVencimiento) : null;
  const horasRestantes = limite ? Math.max(0, Math.ceil((limite.getTime() - Date.now()) / 3600000)) : null;
  const primeraPendiente = cuota.montoPagado <= 0;
  const montoPrimera = Math.min(cuota.primeraCuotaMonto ?? 300, cuota.saldo);
  const configuracion=configQr.data?.configuracion; const rutaQr=tipoQr==="TOTAL"?configuracion?.qrPagoTotal:tipoQr==="PRIMERA"?configuracion?.qrPrimeraCuota:configuracion?.qrSegundaCuota;
  return <div className="min-h-screen bg-[#eee8dc] p-4 sm:p-8"><div className="mx-auto max-w-5xl space-y-6"><header className="rounded-3xl bg-[#74122A] p-6 text-white"><p className="text-xs font-bold uppercase tracking-[.25em] text-[#e9cf91]">Tinkus Puros · Estado financiero</p><h1 className="mt-2 text-3xl font-black">Mi cuota</h1><div className="mt-5 grid gap-3 sm:grid-cols-3"><Resumen titulo="Total" valor={cuota.montoTotal} /><Resumen titulo="Pagado verificado" valor={cuota.montoPagado} /><Resumen titulo="Saldo" valor={cuota.saldo} /></div><Link to="/comunicados" className="mt-5 inline-block text-sm font-bold">← Volver a comunicados</Link></header><PagoLimite limite={limite} horas={horasRestantes} primeraPendiente={primeraPendiente} primera={montoPrimera} saldo={cuota.saldo}/><QrPago configuracion={configuracion} aceptados={Boolean(configQr.data?.terminosAceptados)} tipo={tipoQr} cambiarTipo={setTipoQr} ruta={rutaQr} abrirTerminos={()=>setModalTerminos(true)} modal={modalTerminos} cerrar={()=>setModalTerminos(false)} aceptar={()=>aceptar.mutate()} procesando={aceptar.isPending}/>
    {cuota.saldo > 0 && <form onSubmit={(e) => { e.preventDefault(); pagar.mutate(); }} className="grid gap-4 rounded-2xl bg-white p-5 shadow-sm sm:grid-cols-2"><div className="sm:col-span-2"><h2 className="text-xl font-black text-[#74122A]">Registrar un pago</h2><p className="text-sm text-slate-500">Puedes pagar el saldo en una o varias partes.</p></div><label><Titulo>Monto total de este pago</Titulo><input required type="number" min="0.01" max={cuota.saldo} step="0.01" value={form.monto || ""} onChange={(e) => { const monto = Number(e.target.value); setForm({ ...form, monto, montoEfectivo: form.metodoPago === "EFECTIVO" ? monto : form.montoEfectivo, montoQr: form.metodoPago === "QR" ? monto : form.montoQr }); }} className="input-preregistro" /></label><label><Titulo>Medio de pago</Titulo><select value={form.metodoPago} onChange={(e) => actualizarMetodo(e.target.value as MetodoPago)} className="input-preregistro"><option value="QR">QR</option><option value="EFECTIVO">Efectivo</option><option value="MIXTO">Mixto</option></select></label>{form.metodoPago === "MIXTO" && <><label><Titulo>Monto en efectivo</Titulo><input required type="number" min="0" step="0.01" value={form.montoEfectivo || ""} onChange={(e) => setForm({ ...form, montoEfectivo: Number(e.target.value) })} className="input-preregistro" /></label><label><Titulo>Monto por QR</Titulo><input required type="number" min="0" step="0.01" value={form.montoQr || ""} onChange={(e) => setForm({ ...form, montoQr: Number(e.target.value) })} className="input-preregistro" /></label></>}<label><Titulo>Quién realizó el pago</Titulo><input required value={form.nombrePagador} onChange={(e) => setForm({ ...form, nombrePagador: e.target.value })} className="input-preregistro" /></label><label><Titulo>Fecha del pago</Titulo><input required type="date" value={form.fechaPago} onChange={(e) => setForm({ ...form, fechaPago: e.target.value })} className="input-preregistro" /></label>{form.metodoPago !== "EFECTIVO" && <label className="sm:col-span-2"><Titulo>Imagen del baucher QR</Titulo><input required type="file" accept="image/jpeg,image/png,image/webp" onChange={(e) => setForm({ ...form, baucher: e.target.files?.[0] ?? null })} className="input-preregistro" /></label>}<button disabled={pagar.isPending} className="rounded-xl bg-[#74122A] px-5 py-3 font-bold text-white sm:col-span-2">{pagar.isPending ? "Enviando..." : "Enviar pago para revisión"}</button></form>}
    <section><h2 className="mb-4 text-xl font-black text-[#74122A]">Historial de pagos</h2><div className="grid gap-4 md:grid-cols-2">{pagos.map((p) => <article key={p._id} className="rounded-2xl border bg-white p-5"><div className="flex justify-between"><div><p className="text-xs text-slate-500">Pago N.º {p.numeroPago} · {p.metodoPago}</p><strong className="text-xl">Bs {p.monto.toFixed(2)}</strong></div><span className="text-xs font-bold text-[#74122A]">{p.estadoRevision}</span></div>{p.metodoPago === "MIXTO" && <p className="mt-2 text-sm">Efectivo: Bs {p.montoEfectivo.toFixed(2)} · QR: Bs {p.montoQr.toFixed(2)}</p>}<p className="mt-2 text-sm">Pagó: {p.nombrePagador}</p><p className="text-sm">Fecha: {new Date(p.fechaPago).toLocaleDateString("es-BO")}</p>{p.baucherImagen && <a href={`${API}${p.baucherImagen}`} target="_blank" rel="noreferrer" className="mt-3 inline-block text-sm font-bold text-blue-700">Ver baucher</a>}{p.observacionRevision && <p className="mt-3 rounded-lg bg-amber-50 p-2 text-sm"><strong>Observación:</strong> {p.observacionRevision}</p>}</article>)}</div></section>
  </div></div>;
}
function Resumen({ titulo, valor }: { titulo: string; valor: number }) { return <div className="rounded-xl bg-white/10 p-4"><p className="text-xs text-white/70">{titulo}</p><strong className="text-2xl">Bs {valor.toFixed(2)}</strong></div>; }
function PagoLimite({limite,horas,primeraPendiente,primera,saldo}:{limite:Date|null;horas:number|null;primeraPendiente:boolean;primera:number;saldo:number}){if(!limite||saldo<=0)return null;return <div className={`rounded-xl p-4 font-bold ${horas===0?"bg-red-100 text-red-800":"bg-amber-100 text-amber-900"}`}>{horas===0?"El plazo de pago venció. Comunícate con administración.":`Tienes aproximadamente ${horas} horas para pagar ${primeraPendiente?`la primera cuota de Bs ${primera.toFixed(2)} o la totalidad`:`el saldo de Bs ${saldo.toFixed(2)}`}. Fecha límite: ${limite.toLocaleString("es-BO")}`}</div>}
function Titulo({ children }: { children: React.ReactNode }) { return <span className="mb-1 block text-xs font-bold uppercase text-[#735f55]">{children}</span>; }
function QrPago({configuracion,aceptados,tipo,cambiarTipo,ruta,abrirTerminos,modal,cerrar,aceptar,procesando}:{configuracion:ReturnType<typeof Object>|any;aceptados:boolean;tipo:"TOTAL"|"PRIMERA"|"SEGUNDA";cambiarTipo:(v:"TOTAL"|"PRIMERA"|"SEGUNDA")=>void;ruta?:string;abrirTerminos:()=>void;modal:boolean;cerrar:()=>void;aceptar:()=>void;procesando:boolean}){const[marcado,setMarcado]=useState(false);if(!configuracion)return null;return <><section className="rounded-2xl bg-white p-5 shadow"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="text-xl font-black text-[#74122A]">QR para realizar el pago</h2><p className="text-sm text-slate-500">Acepta los términos antes de visualizar los códigos de pago.</p></div>{!aceptados&&<button onClick={abrirTerminos} className="rounded-xl bg-[#74122A] px-5 py-3 font-bold text-white">Ver términos y habilitar QR</button>}</div>{aceptados&&<div className="mt-5"><div className="grid grid-cols-1 gap-2 sm:grid-cols-3">{([['PRIMERA','Primera cuota · Bs 300'],['SEGUNDA','Segunda cuota · saldo Bs 470 o 550'],['TOTAL','Pago total · Bs 770 o 850']]as const).map(([v,t])=><button key={v} onClick={()=>cambiarTipo(v)} className={`rounded-xl px-3 py-3 text-xs font-bold ${tipo===v?'bg-[#841534] text-white':'bg-[#eee8dc] text-[#5d4a42]'}`}>{t}</button>)}</div>{ruta?<div className="mt-4 text-center"><img src={`${API}${ruta}`} alt={`QR ${tipo.toLowerCase()}`} className="mx-auto max-h-96 w-full max-w-sm object-contain"/><a href={`${API}${ruta}`} target="_blank" rel="noreferrer" className="mt-3 inline-block rounded-xl border px-4 py-2 font-bold text-[#74122A]">Abrir QR</a></div>:<p className="mt-4 rounded-xl bg-amber-50 p-4 text-center font-bold text-amber-800">Administración todavía no cargó este QR.</p>}</div>}</section>{modal&&<div className="fixed inset-0 z-[120] grid place-items-center overflow-y-auto bg-black/65 p-3 sm:p-4"><section className="relative max-h-[calc(100dvh-1.5rem)] w-full max-w-lg overflow-y-auto rounded-3xl bg-white p-5 shadow-2xl sm:p-6"><button onClick={cerrar} aria-label="Cerrar" className="sticky left-full top-0 z-10 grid h-10 w-10 place-items-center rounded-full bg-slate-100 font-black">✕</button><h2 className="-mt-10 pr-12 text-2xl font-black text-[#74122A]">Términos y condiciones de pago</h2><div className="mt-4 max-h-[45dvh] overflow-y-auto whitespace-pre-wrap rounded-xl bg-slate-50 p-4 text-sm leading-6 text-slate-700">{configuracion.terminos}</div><label className="mt-4 flex items-start gap-3 rounded-xl border p-4"><input type="checkbox" checked={marcado} onChange={e=>setMarcado(e.target.checked)} className="mt-1"/><span className="text-sm font-semibold">He leído y acepto los términos y condiciones para visualizar los QR y registrar mi pago.</span></label><div className="mt-5 grid grid-cols-1 gap-3 sm:grid-cols-2"><button onClick={cerrar} className="rounded-xl border px-4 py-3 font-bold">Cancelar</button><button onClick={()=>marcado?aceptar():toast.error("Debes aceptar los términos y condiciones")} disabled={procesando} className="rounded-xl bg-[#74122A] px-4 py-3 font-bold text-white disabled:opacity-50">{procesando?'Guardando...':'Aceptar y mostrar QR'}</button></div></section></div>}</>}
