{
  "name": "tinkus-r2-storage-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.120.0"
  }
}
