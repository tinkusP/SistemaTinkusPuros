import type{Request,Response}from"express";import Gestion from"../models/Gestion";import Preregistro from"../models/Preregistro";import Cuota from"../models/Cuota";import DetalleCuota from"../models/DetalleCuota";import Guia from"../models/Guia";import Bloque from"../models/Bloque";import Fraterno from"../models/Fraterno";import EntregaIndumentaria from"../models/EntregaIndumentaria";import Asistencia from"../models/Asistencia";
const genero=(s?:string)=>["HOMBRE","MASCULINO","M"].includes(String(s||"").toUpperCase())?"HOMBRE":["MUJER","FEMENINO","F"].includes(String(s||"").toUpperCase())?"MUJER":"SIN_REGISTRO";
export async function reporteEjecutivo(req:Request,res:Response){const gestion=req.query.gestionId?await Gestion.findById(req.query.gestionId):await Gestion.findOne({estado:{$in:["ACTIVA","INSCRIPCIONES"]},fechaEliminado:null}).sort({anio:-1});if(!gestion)return res.status(404).json({error:"No existe una gestión para generar reportes"});const preregistros=await Preregistro.find({gestionId:gestion._id,fechaEliminado:null}).populate("usuarioId","nombres apellidoPaterno apellidoMaterno ci sexo facultad carrera tipoOrigen estado fotoPerfil email").lean();const ids=preregistros.map(p=>p._id),cuotas=await Cuota.find({preregistroId:{$in:ids},fechaEliminado:null}).lean(),pagos=await DetalleCuota.find({cuotaId:{$in:cuotas.map(c=>c._id)},estadoRevision:"VERIFICADO",fechaEliminado:null}).sort({numeroPago:1}).lean();const cuotaPorPre=new Map(cuotas.map(c=>[String(c.preregistroId),c])),pagosPorCuota=new Map<string,typeof pagos>();for(const p of pagos){const k=String(p.cuotaId);pagosPorCuota.set(k,[...(pagosPorCuota.get(k)||[]),p]);}const personas=preregistros.map(p=>{const u=p.usuarioId as any,c=cuotaPorPre.get(String(p._id)),ps=c?pagosPorCuota.get(String(c._id))||[]:[];let clasificacion="SIN_CUOTA";if(c){if(!ps.length)clasificacion="SIN_PAGO";else if(c.estado==="PAGADA"&&ps.length===1&&ps[0].metodoPago==="QR")clasificacion="TOTAL_UN_SOLO_QR";else if(c.estado==="PAGADA")clasificacion="PAGADO_TOTAL";else if(ps.length===1)clasificacion="PRIMERA_CUOTA";else clasificacion="SEGUNDA_CUOTA";}return{usuarioId:u?._id,preregistroId:p._id,nombre:[u?.nombres,u?.apellidoPaterno,u?.apellidoMaterno].filter(Boolean).join(" "),ci:u?.ci,email:u?.email,genero:genero(u?.sexo),facultad:u?.facultad||"SIN FACULTAD",carrera:u?.carrera||"SIN CARRERA",origen:u?.tipoOrigen||"SIN ORIGEN",estadoUsuario:u?.estado,estadoPreregistro:p.estado,fotoPerfil:u?.fotoPerfil,cuota:{clasificacion,montoTotal:c?.montoTotal||0,montoPagado:c?.montoPagado||0,saldo:c?.saldo||0,cantidadPagos:ps.length}}});const agrupar=(campo:"genero"|"facultad"|"carrera"|"origen"|"estadoPreregistro")=>Object.entries(personas.reduce<Record<string,number>>((a,p)=>(a[p[campo]||"SIN REGISTRO"]=(a[p[campo]||"SIN REGISTRO"]||0)+1,a),{})).map(([nombre,total])=>({nombre,total})).sort((a,b)=>b.total-a.total);const[guias,fraternos,bloques,entregas,asistencias]=await Promise.all([Guia.countDocuments({gestionId:gestion._id,estado:"ACTIVO"}),Fraterno.countDocuments({gestionId:gestion._id,estado:"ACTIVO",fechaEliminado:null}),Bloque.find({gestionId:gestion._id}).lean(),EntregaIndumentaria.countDocuments({estado:"ENTREGADO"}),Asistencia.countDocuments({gestionId:gestion._id,estado:"PRESENTE"})]);const finanzas={montoEsperado:cuotas.reduce((s,c)=>s+c.montoTotal,0),montoCobrado:cuotas.reduce((s,c)=>s+c.montoPagado,0),saldoPendiente:cuotas.reduce((s,c)=>s+c.saldo,0),sinPago:personas.filter(p=>p.cuota.clasificacion==="SIN_PAGO").length,primeraCuota:personas.filter(p=>p.cuota.clasificacion==="PRIMERA_CUOTA").length,segundaCuota:personas.filter(p=>p.cuota.clasificacion==="SEGUNDA_CUOTA").length,totalUnQr:personas.filter(p=>p.cuota.clasificacion==="TOTAL_UN_SOLO_QR").length,pagadoTotal:personas.filter(p=>["TOTAL_UN_SOLO_QR","PAGADO_TOTAL"].includes(p.cuota.clasificacion)).length};return res.json({generadoEn:new Date(),gestion:{_id:gestion._id,nombre:gestion.nombre,anio:gestion.anio},resumen:{total:personas.length,hombres:personas.filter(p=>p.genero==="HOMBRE").length,mujeres:personas.filter(p=>p.genero==="MUJER").length,pendientes:personas.filter(p=>p.estadoPreregistro==="PENDIENTE").length,aprobados:personas.filter(p=>p.estadoPreregistro==="APROBADO").length,fraternos,guias,bloques:bloques.length,capacidadBloques:bloques.reduce((s,b:any)=>s+b.filasHombres*b.columnasHombres+b.filasMujeres*b.columnasMujeres,0),entregas,asistencias},finanzas,distribuciones:{genero:agrupar("genero"),facultad:agrupar("facultad"),carrera:agrupar("carrera"),origen:agrupar("origen"),estadoPreregistro:agrupar("estadoPreregistro")},personas});}
