const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs"),path=require("node:path");
const leer=(p)=>fs.readFileSync(path.join(__dirname,"..",p),"utf8");
test("control financiero usa únicamente asignaciones y bloques activos",()=>{const s=leer("src/services/ControlFinancieroBloquesService.ts");assert.match(s,/DetalleBloque\.find\(\{estado:"ACTIVO",fechaEliminado:null/);assert.match(s,/Bloque\.find\(\{estado:"ACTIVO"/);assert.match(s,/seen\.has/)});
test("exención es explícita, auditada y no cambia la historia de pagos",()=>{const m=leer("src/models/Cuota.ts"),c=leer("src/controllers/CuotaController.ts"),r=leer("src/routes/CuotaRoutes.ts");assert.match(m,/exentoPago/);assert.match(c,/MARCAR_EXENTO/);assert.match(c,/RETIRAR_EXENCION/);assert.match(r,/soloAdministradorReal/);assert.doesNotMatch(c,/DetalleCuota\.delete/)});
test("pendientes, verificados y exentos se calculan por separado",()=>{const s=leer("src/services/ControlFinancieroBloquesService.ts");assert.match(s,/estadoRevision==="VERIFICADO"/);assert.match(s,/estadoRevision==="PENDIENTE"/);assert.match(s,/montoRealEsperado:esperado/);assert.match(s,/saldoPorCobrar:sum\(ob,"saldo"\)/)});
